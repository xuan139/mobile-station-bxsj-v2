[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "mobile-station-bxsj-v2"
version = "0.1.0"
description = "與綠能完全隔離的 BXSJ V2 預留骨架"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]

